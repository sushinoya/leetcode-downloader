# Leetcode Downloader

```shell
python3 fetch_submissions.py [-h] [--retry_count RETRY_COUNT]

A simple script to download your fastest, accepted solutions from Leetcode!

optional arguments:
  -h, --help            show this help message and exit
  --retry_count RETRY_COUNT
                        maximum number of incorrect logins allowed
```
